# Runtime-only dependencies for the read-only Streamlit dashboard (e.g. Streamlit
# Community Cloud). The dashboard reads frozen JSON caches (report cache,
# explanations cache, wafer narratives), so the heavy modelling stack used only by
# the offline build CLIs -- pymc, numpyro, jax, pyHSICLasso, shap, dbt-duckdb -- is
# intentionally excluded. To regenerate the caches, install the full environment
# from pyproject.toml instead.
streamlit==1.57.0
pandas==3.0.3
numpy==2.4.5
plotly==6.7.0
scikit-learn==1.8.0
feature-engine==1.9.4
duckdb==1.5.2
